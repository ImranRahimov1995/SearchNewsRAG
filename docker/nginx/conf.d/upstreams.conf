# Nginx upstream configuration
# Defines backend service groups with health checks and load balancing

upstream backend {
    server backend:8000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

upstream frontend {
    server frontend:80 max_fails=3 fail_timeout=30s;
    keepalive 32;
}
